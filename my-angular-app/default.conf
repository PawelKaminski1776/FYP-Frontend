server {
    listen 80;
    server_name InspectionAnalysisApp;

    root /usr/share/nginx/html/browser  # Point to the correct directory
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;  # Ensure routing works properly
    }

    error_page 404 /index.html;
}
